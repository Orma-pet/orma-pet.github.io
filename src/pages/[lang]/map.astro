---
import MapPage from '../../components/MapPage.astro';
import { ROUTES, LANGS_DINAMICHE } from '../../data/site.js';
export function getStaticPaths() {
  return LANGS_DINAMICHE.map((l) => ({ params: { lang: l.code } }));
}
const { lang } = Astro.params;
---
<MapPage lang={lang} />
