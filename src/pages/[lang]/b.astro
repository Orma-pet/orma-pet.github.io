---
import BuongiornoPage from '../../components/BuongiornoPage.astro';
import { LANGS_DINAMICHE } from '../../data/site.js';
export function getStaticPaths() {
  return LANGS_DINAMICHE.map((l) => ({ params: { lang: l.code } }));
}
const { lang } = Astro.params;
---
<BuongiornoPage lang={lang} />
